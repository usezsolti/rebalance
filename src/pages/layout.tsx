// src/app/layout.tsx (vagy src/pages/layout.tsx a projekt szerkezetétől függően)
import './globals.css'; // Globális stílusok importálása


export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="hu">
    </html>
  );
}
